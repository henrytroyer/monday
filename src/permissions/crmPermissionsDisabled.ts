/**
 * crmPermissionsDisabled.ts — Kill-switch for Portal Things RBAC in the CRM UI.
 *
 * When true: every operator sees the full CRM; Users / Roles & permissions
 * menus are removed separately in navItems. No per-page permission checks.
 */
export const CRM_PERMISSIONS_DISABLED = true;
